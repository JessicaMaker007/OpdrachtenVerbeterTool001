import { defineConfig } from 'vite';

// Configuratie voor je project
export default defineConfig({
  // Als je bijvoorbeeld een aangepaste server wilt instellen:
  server: {
    open: true // Dit opent automatisch de browser bij het starten van Vite
  },
  // Hier kun je extra configuraties toevoegen als dat nodig is
});
